- 诊断了 `C:\Users\Administrator\Documents\ChatGPT\openai转claude 2` 的 JetLinks→Claude Code 本地桥接。确认 Docker 29.7.2、Claude Code 2.1.140、LiteLLM 1.102.0；容器与 shim 健康。本地 `/v1/messages` 的非流式、SSE 流式、工具调用和 Claude Code `--print` 端到端测试均成功。历史错误的直接原因是用户级 `~/.claude/settings.json` 将 `ANTHROPIC_BASE_URL` 覆盖为另一个远端地址；启动脚本使用 `--setting-sources project,local` 时可避开该覆盖。发现 Claude Code 最新版本为 2.1.280，当前版本较旧，但不是这次失败的根因。用户粘贴的 API Key 已暴露，建议撤销轮换。
- 经用户授权，将 `C:\Users\Administrator\.claude\settings.json` 和 `config.json` 的网关地址切换到 `http://127.0.0.1:4000`，默认及 Opus/Sonnet/Haiku 映射统一设为 `gpt-5.5`。修改前备份为 `settings.json.backup-20260923-124614`、`config.json.backup-20260923-124614`。使用纯用户配置执行 Claude Code 端到端测试返回 `JETLINKS_OK`，会话记录确认实际模型为 `gpt-5.5`，容器日志确认 `/v1/messages?beta=true` 返回 200。
- 查询 JetLinks `/v1/models` 后确认用户所称 `gpt-6sol` 的准确 ID 为 `gpt-5.6-sol`。已在 LiteLLM 中新增该映射并保留 `gpt-5.5` 回退，将启动脚本、验证脚本、README 与 Claude Code 默认/别名映射切换到 `gpt-5.6-sol`；备份时间戳为 `20260923-134836`。重启桥接后，直连上游、非流式、SSE、工具调用、Claude Code 端到端均通过，会话记录确认实际模型 `gpt-5.6-sol`。
