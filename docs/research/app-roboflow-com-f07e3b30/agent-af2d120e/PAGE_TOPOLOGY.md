# Agent page topology

1. Fixed-height application shell filling the viewport.
2. Left navigation rail, desktop width `60px` when collapsed and `155px` when expanded.
3. Main Codezzn workspace; all existing page content and hash routes remain unchanged.
4. Mobile navigation remains an off-canvas drawer.

The requested desktop interaction is hover/focus-driven expansion. A manual pin control remains available for keyboard and touch users.

