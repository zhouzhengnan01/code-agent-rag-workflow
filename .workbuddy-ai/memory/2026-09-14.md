- Continued the requested hardening tranche: added Windows taskkill process-tree timeout handling, persisted turn_events schema and SSE event recording/replay query support, request disconnect cancellation, approval inbox endpoint, thread artifact schema, and expanded workflow node validation. Backend compilation and git diff checks pass. The broad requested scope (true container sandbox, approval continuation, multi-file transactional rollback, full AST/incremental repository RAG, full E2E suite) remains incomplete and needs staged implementation rather than claiming complete parity.
- Completed Shell/Git per-invocation Docker isolation: separate trusted broker with socket, authenticated restricted API, one workspace mount, UID/GID 10001, none network, read-only rootfs, CPU/memory/PID/output limits, independent PID-1 deadline, cancellation/removal, persisted task states and instance-filtered orphan recovery. Cleanup pending blocks new execution; no host fallback. Deployment via docker-compose.sandbox.yml and Dockerfile.sandbox; existing .env and business data preserved. Native file tools/MCP, disk quotas, shared workspace concurrency, approvals/transactions remain out of scope.
- Real Docker Desktop Linux validation passed eight groups (isolation, write persistence in scratch volume, resource policy/cancel, timeout child cleanup, actual OOM, actual PID limit, independent watchdog, startup recovery). Core + broker + HTTP client integration regression: 30 passed. Docker Engine works here but Compose plugin unavailable, so combined deployment not exercised. Built codezzn-sandbox:local. Added reusable verification scripts tests/test_sandbox.py, tests/test_sandbox_client.py, tests/sandbox_live.py and user acceptance guide outputs/sandbox-acceptance.html.
- Added sandbox_mode selector to web/workbench.js agent editor (read-only/workspace-write); missing values default read-only on save, unsupported legacy modes require explicit selection. Kept allow_shell/auto_approve independent and fixed missing allow_shell UI default to false. Node syntax and isolated form-render/save tests pass; browser E2E not run. Container users must rebuild copied web assets and refresh; existing agent settings are not modified until saved.
