# Technical Stack Analysis

The source is a React/Firebase authentication UI with compiled CSS, Inter, Firebase provider icons, HTML video and CSS 3D animation. The Codezzn equivalent uses semantic static HTML, scoped CSS, vanilla JavaScript and existing FastAPI authentication endpoints. This avoids importing Firebase while preserving the visual and interaction model.
